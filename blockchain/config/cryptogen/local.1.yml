# ---------------------------------------------------------------------------
# "PeerOrgs" - Definition of organizations managing peer nodes
# ---------------------------------------------------------------------------
PeerOrgs:
    - Name: Local1
      Domain: id1.local.healthchain.com
      EnableNodeOUs: true
      Template:
          Count: 1
          SANS:
              - peer0.id1.local.healthchain.com
      Users:
          Count: 1
